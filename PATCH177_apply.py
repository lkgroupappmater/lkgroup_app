from pathlib import Path
import shutil, re
cwd=Path.cwd()
def rd(p): return p.read_text(encoding='utf-8-sig')
def wr(p,s): p.write_text(s,encoding='utf-8')
shutil.copy2(cwd/'receipt_discount_service.txt', cwd/'lib/services/receipt_discount_service.dart')
p=cwd/'lib/services/customer_benefit_service.dart'
s=rd(p)
s=s.replace("      destinationAddress,\n      paidBy,\n    ].where((e) => e.trim().isNotEmpty).join(', ');", "      destinationAddress,\n    ].where((e) => e.trim().isNotEmpty).join(', ');", 1)
old='  static bool _nameMatches(\n    String shipmentName,\n    Iterable<String> candidates,\n  ) {\n    final shipmentTokens = _nameTokens(shipmentName).toSet();\n    if (shipmentTokens.isEmpty) return false;\n\n    for (final value in candidates) {\n      if (value.trim().isEmpty) continue;\n      final candidateTokens = _nameTokens(value).toSet();\n      if (shipmentTokens.any(candidateTokens.contains)) return true;\n    }\n    return false;\n  }'
new='  static int _nameMatchRank(String shipmentName, String candidateName) {\n    final a = _nameTokens(shipmentName).toSet();\n    final b = _nameTokens(candidateName).toSet();\n    if (a.isEmpty || b.isEmpty) return 9999;\n    final overlap = a.intersection(b);\n    if (overlap.isEmpty) return 9999;\n    if (a.length == b.length && a.containsAll(b) && b.containsAll(a)) return 0;\n    if (a.containsAll(b)) return 10 + (a.length - b.length);\n    if (b.containsAll(a)) return 30 - b.length.clamp(0, 20);\n    return 50 - overlap.length.clamp(0, 20);\n  }\n\n  static int _bestNameRank(\n    String shipmentName,\n    Iterable<String> candidates,\n  ) {\n    var best = 9999;\n    for (final value in candidates) {\n      if (value.trim().isEmpty) continue;\n      final rank = _nameMatchRank(shipmentName, value);\n      if (rank < best) best = rank;\n    }\n    return best;\n  }'
if old in s: s=s.replace(old,new,1)
old='    for (final raw in rows) {\n      final rule =\n          LocalDeliveryRule.fromMap(Map<String, dynamic>.from(raw));\n      if (!_phoneMatches(phone, rule.phone)) continue;\n      if (!_nameMatches(\n        name,\n        [rule.customerName, rule.alternateName, rule.companyName],\n      )) {\n        continue;\n      }\n      return rule;\n    }\n    return null;'
new='    LocalDeliveryRule? bestRule;\n    var bestRank = 9999;\n    var bestSource = 1 << 30;\n    for (final raw in rows) {\n      final rule =\n          LocalDeliveryRule.fromMap(Map<String, dynamic>.from(raw));\n      if (!_phoneMatches(phone, rule.phone)) continue;\n      final rank = _bestNameRank(\n        name,\n        [rule.customerName, rule.alternateName, rule.companyName],\n      );\n      if (rank >= 9999) continue;\n      final source = rule.sourceNo ?? (1 << 30);\n      if (rank < bestRank || (rank == bestRank && source < bestSource)) {\n        bestRule = rule;\n        bestRank = rank;\n        bestSource = source;\n      }\n    }\n    return bestRule;'
if old not in s: raise SystemExit('CustomerBenefit matching anchor missing')
s=s.replace(old,new,1)
wr(p,s)
p=cwd/'lib/services/freight_service.dart'
s=rd(p)
s=s.replace("      final voyage = '${row['voyage'] ?? ''}';\n\n      final cacheKey =\n          '$routeKey|${year ?? ''}|$voyage|${_digits(phone)}|${name.trim().toLowerCase()}';", "      final voyage = '${row['voyage'] ?? ''}';\n      final receiptNumber = '${row['receipt_number'] ?? ''}'.trim();\n\n      final cacheKey =\n          '$routeKey|${year ?? ''}|$voyage|$receiptNumber|${_digits(phone)}|${name.trim().toLowerCase()}';", 1)
s=s.replace("              voyage: voyage,\n              name: name,\n              phone: phone,\n            );", "              voyage: voyage,\n              receiptNumber: receiptNumber,\n              name: name,\n              phone: phone,\n            );", 1)
s=s.replace("    required String voyage,\n    required String name,\n    required String phone,\n  }) async {", "    required String voyage,\n    required String receiptNumber,\n    required String name,\n    required String phone,\n  }) async {", 1)
ins="    if (year != null && receiptNumber.isNotEmpty) {\n      try {\n        final rawManual = await SupabaseService.client.rpc(\n          'get_receipt_discount_override',\n          params: {\n            'p_route_key': routeKey,\n            'p_year': year,\n            'p_voyage': voyage,\n            'p_receipt_number': receiptNumber,\n          },\n        );\n        if (rawManual is Map) {\n          final manual = Map<String, dynamic>.from(rawManual);\n          if (manual['id'] != null) {\n            return {\n              'id': manual['id'],\n              'discount_percent': manual['discount_percent'] ?? 0,\n              'group_name': manual['discount_name'] ?? '특별할인',\n              'customer_name': name,\n              'source_detail': '영수번호 수동 할인',\n            };\n          }\n        }\n      } catch (_) {}\n    }\n\n"
anchor='    // Patch123+: Customer Group의 개인명+회사명 물량을 같은 항차에서 합산해\n    // bulk tier를 결정합니다.'
if ins not in s:
    if anchor not in s: raise SystemExit('Freight manual discount anchor missing')
    s=s.replace(anchor,ins+anchor,1)
wr(p,s)
p=cwd/'lib/services/excel_import_service.dart'
s=rd(p)
s=s.replace('for (var rr = headerRow; rr >= 0 && rr >= headerRow - 3; rr--) {','for (var rr = headerRow; rr >= 0 && rr >= headerRow - 12; rr--) {',1)
wr(p,s)
p=cwd/'supabase/functions/export-shipment-excel/index.ts'
s=rd(p)
s=s.replace("d.destination_address, d.paid_by].filter(Boolean).join(', ')","d.destination_address].filter(Boolean).join(', ')",1)
wr(p,s)
p=cwd/'lib/screens/cargo_management_screen.dart'
s=rd(p)
s=s.replace("import '../services/receipt_extra_cost_service.dart';","import '../services/receipt_extra_cost_service.dart';\nimport '../services/receipt_discount_service.dart';",1)
method="  Future<void> _showReceiptDiscountDialog(\n    List<Map<String, dynamic>> rows,\n  ) async {\n    if (rows.isEmpty || !(_isAdmin || _isStaff)) return;\n    final first = rows.first;\n    final routeKey =\n        RouteCatalog.formRouteKeyFor('${first['route'] ?? ''}'.trim());\n    final year = (first['shipment_year'] as num?)?.toInt();\n    final voyage = '${first['voyage'] ?? ''}'.trim();\n    final receipt = '${first['receipt_number'] ?? ''}'.trim();\n    if (routeKey.isEmpty || year == null || voyage.isEmpty || receipt.isEmpty) {\n      _message('할인을 연결할 영수번호 정보를 확인할 수 없습니다.');\n      return;\n    }\n\n    var current = await ReceiptDiscountService.instance.get(\n      routeKey: routeKey,\n      year: year,\n      voyage: voyage,\n      receiptNumber: receipt,\n    );\n    if (!mounted) return;\n\n    final discountName =\n        TextEditingController(text: current?.discountName ?? '특별할인');\n    final percent = TextEditingController(\n      text: current == null\n          ? ''\n          : (current.discountPercent * 100)\n              .toStringAsFixed(2)\n              .replaceFirst(RegExp(r'\\.00$'), ''),\n    );\n\n    await showDialog<void>(\n      context: context,\n      builder: (dialogContext) => StatefulBuilder(\n        builder: (context, setDialogState) => AlertDialog(\n          title: Text('$receipt · 할인 적용'),\n          content: SizedBox(\n            width: 420,\n            child: Column(\n              mainAxisSize: MainAxisSize.min,\n              children: [\n                if (current != null)\n                  Text(\n                    '현재: ${current!.discountName} '\n                    '${(current!.discountPercent * 100).toStringAsFixed(2).replaceFirst(RegExp(r'\\.00$'), '')}%',\n                    style: const TextStyle(fontWeight: FontWeight.bold),\n                  ),\n                const SizedBox(height: 8),\n                TextField(\n                  controller: discountName,\n                  decoration: const InputDecoration(\n                    labelText: '할인명 / 단체명',\n                    hintText: '예: 라선협, 기업 할인, 특별할인',\n                    border: OutlineInputBorder(),\n                  ),\n                ),\n                const SizedBox(height: 8),\n                TextField(\n                  controller: percent,\n                  keyboardType:\n                      const TextInputType.numberWithOptions(decimal: true),\n                  decoration: const InputDecoration(\n                    labelText: '할인율',\n                    suffixText: '%',\n                    border: OutlineInputBorder(),\n                  ),\n                ),\n              ],\n            ),\n          ),\n          actions: [\n            if (current != null)\n              TextButton.icon(\n                onPressed: () async {\n                  await ReceiptDiscountService.instance.delete(\n                    routeKey: routeKey,\n                    year: year,\n                    voyage: voyage,\n                    receiptNumber: receipt,\n                  );\n                  if (dialogContext.mounted) Navigator.pop(dialogContext);\n                  _message('$receipt 할인 적용을 삭제했습니다.');\n                },\n                icon: const Icon(Icons.delete_outline),\n                label: const Text('삭제'),\n              ),\n            TextButton(\n              onPressed: () => Navigator.pop(dialogContext),\n              child: const Text('닫기'),\n            ),\n            FilledButton.icon(\n              onPressed: () async {\n                final value = double.tryParse(percent.text.trim());\n                if (value == null || value < 0 || value > 100) {\n                  ScaffoldMessenger.of(context).showSnackBar(\n                    const SnackBar(content: Text('할인율은 0~100 사이로 입력해 주세요.')),\n                  );\n                  return;\n                }\n                await ReceiptDiscountService.instance.save(\n                  routeKey: routeKey,\n                  year: year,\n                  voyage: voyage,\n                  receiptNumber: receipt,\n                  discountName: discountName.text.trim().isEmpty\n                      ? '특별할인'\n                      : discountName.text.trim(),\n                  discountPercent: value / 100,\n                );\n                current = await ReceiptDiscountService.instance.get(\n                  routeKey: routeKey,\n                  year: year,\n                  voyage: voyage,\n                  receiptNumber: receipt,\n                );\n                if (dialogContext.mounted) setDialogState(() {});\n                _message('$receipt 할인 적용 완료');\n              },\n              icon: const Icon(Icons.percent),\n              label: Text(current == null ? '적용' : '수정 저장'),\n            ),\n          ],\n        ),\n      ),\n    );\n\n    await Future<void>.delayed(const Duration(milliseconds: 350));\n    discountName.dispose();\n    percent.dispose();\n    if (mounted) setState(() {});\n  }\n\n"
anchor='  Future<void> _showReceiptExtraCostDialog(\n    List<Map<String, dynamic>> rows,\n  ) async {'
if '_showReceiptDiscountDialog(' not in s:
    if anchor not in s: raise SystemExit('extra cost dialog anchor missing')
    s=s.replace(anchor,method+anchor,1)
button="                if ((_isAdmin || _isStaff) && receipt.isNotEmpty)\n                  FutureBuilder<ReceiptDiscountOverride?>(\n                    future: ReceiptDiscountService.instance.get(\n                      routeKey: RouteCatalog.formRouteKeyFor(\n                        '${first['route'] ?? ''}',\n                      ),\n                      year: (first['shipment_year'] as num?)?.toInt() ?? 0,\n                      voyage: '${first['voyage'] ?? ''}'.trim(),\n                      receiptNumber: receipt,\n                    ),\n                    builder: (context, snapshot) {\n                      final rule = snapshot.data;\n                      final pct = rule == null\n                          ? ''\n                          : '${(rule.discountPercent * 100).toStringAsFixed(2).replaceFirst(RegExp(r'\\.00$'), '')}%';\n                      return Row(\n                        mainAxisSize: MainAxisSize.min,\n                        children: [\n                          if (pct.isNotEmpty)\n                            Text(\n                              pct,\n                              style: const TextStyle(\n                                fontSize: 10,\n                                fontWeight: FontWeight.w800,\n                                color: AppColors.navyPrimary,\n                              ),\n                            ),\n                          IconButton(\n                            tooltip: rule == null ? '할인 적용' : '할인 편집 / 삭제',\n                            visualDensity: VisualDensity.compact,\n                            onPressed: _busy\n                                ? null\n                                : () => _showReceiptDiscountDialog(rows),\n                            icon: const Icon(Icons.percent, size: 21),\n                          ),\n                        ],\n                      );\n                    },\n                  ),\n"
anchor2="                if ((_isAdmin || _isStaff) && receipt.isNotEmpty)\n                  FutureBuilder<List<ExtraCostItem>>("
if "tooltip: rule == null ? '할인 적용'" not in s:
    if anchor2 not in s: raise SystemExit('receipt extra button anchor missing')
    s=s.replace(anchor2,button+anchor2,1)
wr(p,s)
p=cwd/'lib/screens/statement_preview_dialog.dart'
s=rd(p)
start=s.find('    final freightGroups = freight.lines')
end=s.find('    final remarkText =', start)
if start<0 or end<0: raise SystemExit('statement remark discount block not found')
block="    final freightGroups = freight.lines\n        .map((line) => line.discountGroup.trim())\n        .where((value) => value.isNotEmpty && value != '기타 할인')\n        .toSet()\n        .toList(growable: false);\n    final freightDiscountPercent = freight.lines\n        .map((line) => line.discountPercent)\n        .fold<double>(0, (best, value) => value > best ? value : best);\n\n    String pctText(double value) {\n      final p = value * 100;\n      return (p - p.roundToDouble()).abs() < .001\n          ? p.toStringAsFixed(0)\n          : p.toStringAsFixed(2);\n    }\n\n    var displayAutoNotes = autoNotes;\n    if (freightDiscountPercent > 0) {\n      final group = freightGroups.isEmpty ? '할인' : freightGroups.first;\n      final phrase = group.contains('할인')\n          ? '$group ${pctText(freightDiscountPercent)}% 적용'\n          : '$group 할인 ${pctText(freightDiscountPercent)}% 적용';\n      final parts = displayAutoNotes.isEmpty\n          ? <String>[]\n          : displayAutoNotes.split(' / ').map((e) => e.trim()).toList();\n      final oldDiscount = parts.indexWhere(\n        (e) => e.contains('할인') && e.contains('% 적용'),\n      );\n      if (oldDiscount >= 0) {\n        parts[oldDiscount] = phrase;\n      } else {\n        final delivery = parts.indexWhere(\n          (e) => e.contains('지방배송') || e.contains('시내배송'),\n        );\n        if (delivery >= 0) {\n          parts.insert(delivery, phrase);\n        } else {\n          parts.add(phrase);\n        }\n      }\n      displayAutoNotes = parts.where((e) => e.isNotEmpty).join(' / ');\n    }\n"
s=s[:start]+block+s[end:]
s=s.replace('final remarkText = autoNotes.isEmpty','final remarkText = displayAutoNotes.isEmpty',1)
s=s.replace(": '${docText.remark}\\n\\n$autoNotes';", ": '${docText.remark}\\n\\n$displayAutoNotes';",1)
old="""    final discountGroupText = freight.lines\n        .map((line) => line.discountGroup.trim())\n        .firstWhere(\n          (value) => value.isNotEmpty,\n          orElse: () => '',\n        );"""
new="""    final discountGroupText = freight.lines\n        .map((line) => line.discountGroup.trim())\n        .firstWhere(\n          (value) => value.isNotEmpty,\n          orElse: () => '',\n        );\n    final actualDiscountPercent = freight.lines\n        .map((line) => line.discountPercent)\n        .fold<double>(0, (best, value) => value > best ? value : best);\n    final actualDiscountPctText =\n        actualDiscountPercent > 0 ? '${pctText(actualDiscountPercent)}%' : '';\n    final discountLabel = actualDiscountPctText.isEmpty\n        ? '할인'\n        : '${discountGroupText.isEmpty ? '할인' : discountGroupText} '\n            '$actualDiscountPctText';"""
if old in s: s=s.replace(old,new,1)
s=s.replace("discountPercentText.isEmpty ? '할인' : '할인 $discountPercentText',","discountLabel,",1)
s=s.replace("discountPercentText.isEmpty\n          ? '특별할인'\n          : '특별할인 $discountPercentText',","isSpecialDiscount && actualDiscountPctText.isNotEmpty\n          ? '$discountGroupText $actualDiscountPctText'\n          : '특별할인',",1)
wr(p,s)
(cwd/'receipt_discount_service.txt').unlink(missing_ok=True)
print('Patch177 applied.')