from pathlib import Path
import shutil
cwd=Path.cwd()
upload=cwd/'lib'/'screens'/'excel_upload_screen.dart'
menu=cwd/'lib'/'screens'/'management_menu_screen.dart'
src=cwd/'unloading_list_management_screen.dart'
dst=cwd/'lib'/'screens'/'unloading_list_management_screen.dart'
assert upload.exists() and menu.exists() and src.exists(), '프로젝트 최상위에서 실행하세요.'
s=upload.read_text(encoding='utf-8-sig')
pairs=[("import '../core/app_colors.dart';\nimport '../services/excel_import_queue_service.dart';","import '../core/app_colors.dart';\nimport '../core/route_catalog.dart';\nimport '../services/excel_import_queue_service.dart';\nimport '../services/excel_bulk_management_service.dart';\nimport '../services/receipt_settlement_service.dart';\nimport '../services/settlement_snapshot_service.dart';\nimport '../services/supabase_service.dart';"),('  final _queue = ExcelImportQueueService.instance;\n  String _message =',"  final _queue = ExcelImportQueueService.instance;\n  List<ExcelBulkBatch> _batches = const [];\n  String? _route;\n  int? _year;\n  String? _voyage;\n  bool _loadingBatches = true;\n  bool _recalculating = false;\n  String _recalculateMessage = '';\n\n  String _message ="),('    _queue.addListener(_refresh);\n  }','    _queue.addListener(_refresh);\n    _loadBatches();\n  }')]
for old,new in pairs:
    if s.count(old)!=1: raise SystemExit(f'anchor expected 1, found {s.count(old)}: {old[:40]}')
    s=s.replace(old,new,1)
methods="  List<String> get _routes => _batches\n      .map((e) => e.route)\n      .where((e) => e.isNotEmpty)\n      .toSet()\n      .toList()\n    ..sort();\n\n  List<int> get _years {\n    final values = _batches\n        .where((e) => _route == null || e.route == _route)\n        .map((e) => e.year)\n        .where((e) => e > 0)\n        .toSet()\n        .toList()\n      ..sort((a, b) => b.compareTo(a));\n    return values;\n  }\n\n  List<String> get _voyages {\n    final values = _batches\n        .where((e) =>\n            (_route == null || e.route == _route) &&\n            (_year == null || e.year == _year))\n        .map((e) => e.voyage)\n        .where((e) => e.isNotEmpty)\n        .toSet()\n        .toList()\n      ..sort();\n    return values;\n  }\n\n  Future<void> _loadBatches() async {\n    try {\n      final batches = await ExcelBulkManagementService.instance.listBatches();\n      if (!mounted) return;\n      setState(() {\n        _batches = batches;\n        _loadingBatches = false;\n      });\n    } catch (error) {\n      if (!mounted) return;\n      setState(() {\n        _loadingBatches = false;\n        _recalculateMessage = '현재 데이터 목록 조회 실패: $error';\n      });\n    }\n  }\n\n  Future<void> _recalculateSelected() async {\n    if (_route == null || _year == null || _voyage == null || _recalculating) {\n      return;\n    }\n    setState(() {\n      _recalculating = true;\n      _recalculateMessage = '현재 DB 기준으로 재연산 및 Update 중...';\n    });\n\n    try {\n      await SupabaseService.client.rpc(\n        'admin_finalize_excel_batch_rules',\n        params: {\n          'p_route': _route,\n          'p_year': _year,\n          'p_voyage': _voyage,\n          'p_resequence': true,\n        },\n      );\n\n      final rows = await ExcelBulkManagementService.instance.listRows(\n        route: _route!,\n        year: _year!,\n        voyage: _voyage!,\n      );\n      final settlement =\n          await ReceiptSettlementService.instance.calculate(rows);\n      final routeKey = RouteCatalog.keyFor(_route!);\n      await SettlementSnapshotService.instance.save(\n        routeKey: routeKey,\n        routeLabel: _route!,\n        year: _year!,\n        voyage: _voyage!,\n        settlement: settlement,\n      );\n\n      if (!mounted) return;\n      setState(() {\n        _recalculateMessage =\n            '${_route!} · ${_year!}년 · ${_voyage!}항차 재연산 및 Update 완료';\n      });\n    } catch (error) {\n      if (!mounted) return;\n      setState(() => _recalculateMessage = '재연산 및 Update 실패: $error');\n    } finally {\n      if (mounted) setState(() => _recalculating = false);\n    }\n  }\n\n"
anchor='  Future<void> _pick() async {'
if s.count(anchor)!=1: raise SystemExit('_pick anchor missing')
s=s.replace(anchor,methods+anchor,1)
s=s.replace("title: const Text('화물 데이타 엑셀 업로드'),","title: const Text('화물 데이타 엑셀 업로드 및 Update'),",1)
ui="              const Divider(height: 34),\n              const Text(\n                '현재 데이터 재연산 및 Update',\n                style: TextStyle(\n                  fontSize: 17,\n                  fontWeight: FontWeight.bold,\n                  color: AppColors.primary,\n                ),\n              ),\n              const SizedBox(height: 7),\n              const Text(\n                '직접 데이터 수정 또는 고객/배송/할인 규칙 변경 후 반영이 의심될 때 '\n                '선택 항차의 영수번호·구획·자동 특이사항·할인/정산을 다시 계산합니다.',\n                style: TextStyle(\n                  fontSize: 12,\n                  color: AppColors.textSecondary,\n                ),\n              ),\n              const SizedBox(height: 10),\n              if (_loadingBatches)\n                const LinearProgressIndicator()\n              else ...[\n                DropdownButtonFormField<String>(\n                  value: _route,\n                  isExpanded: true,\n                  decoration: const InputDecoration(\n                    labelText: '운송 경로',\n                    border: OutlineInputBorder(),\n                  ),\n                  items: _routes\n                      .map((e) => DropdownMenuItem(value: e, child: Text(e)))\n                      .toList(),\n                  onChanged: _recalculating\n                      ? null\n                      : (value) {\n                          setState(() {\n                            _route = value;\n                            _year = null;\n                            _voyage = null;\n                          });\n                        },\n                ),\n                const SizedBox(height: 8),\n                DropdownButtonFormField<int>(\n                  value: _year,\n                  decoration: const InputDecoration(\n                    labelText: '년도',\n                    border: OutlineInputBorder(),\n                  ),\n                  items: _years\n                      .map((e) =>\n                          DropdownMenuItem(value: e, child: Text('$e년')))\n                      .toList(),\n                  onChanged: _route == null || _recalculating\n                      ? null\n                      : (value) {\n                          setState(() {\n                            _year = value;\n                            _voyage = null;\n                          });\n                        },\n                ),\n                const SizedBox(height: 8),\n                DropdownButtonFormField<String>(\n                  value: _voyage,\n                  decoration: const InputDecoration(\n                    labelText: '항차',\n                    border: OutlineInputBorder(),\n                  ),\n                  items: _voyages\n                      .map((e) => DropdownMenuItem(\n                            value: e,\n                            child: Text('${e}항차'),\n                          ))\n                      .toList(),\n                  onChanged: _year == null || _recalculating\n                      ? null\n                      : (value) => setState(() => _voyage = value),\n                ),\n                const SizedBox(height: 10),\n                FilledButton.icon(\n                  onPressed: _route == null ||\n                          _year == null ||\n                          _voyage == null ||\n                          _recalculating\n                      ? null\n                      : _recalculateSelected,\n                  icon: _recalculating\n                      ? const SizedBox(\n                          width: 17,\n                          height: 17,\n                          child: CircularProgressIndicator(strokeWidth: 2),\n                        )\n                      : const Icon(Icons.sync),\n                  label: const Text('재연산 및 Update'),\n                ),\n              ],\n              if (_recalculateMessage.isNotEmpty) ...[\n                const SizedBox(height: 8),\n                Text(\n                  _recalculateMessage,\n                  style: const TextStyle(\n                    fontSize: 12,\n                    color: AppColors.textSecondary,\n                  ),\n                ),\n              ],\n              const SizedBox(height: 10),\n"
ui_anchor='              if (_queue.jobs.isNotEmpty) ...['
if s.count(ui_anchor)!=1: raise SystemExit('upload UI anchor missing')
s=s.replace(ui_anchor,ui+ui_anchor,1)
upload.write_text(s,encoding='utf-8')
m=menu.read_text(encoding='utf-8-sig')
old="import 'local_delivery_management_screen.dart';"
new=old+"\nimport 'unloading_list_management_screen.dart';"
if m.count(old)!=1: raise SystemExit('menu import anchor missing')
m=m.replace(old,new,1)
label="'label': '화물 데이타 엑셀 업로드',"
if m.count(label)!=1: raise SystemExit('menu label anchor missing')
m=m.replace(label,"'label': '화물 데이타 엑셀 업로드 및 Update',",1)
ma="        {\n          'label': '공지 및 안내 관리',\n          'icon': Icons.campaign_outlined,\n          'page': NoticeManagementScreen(user: user),\n        },"
mn="        {\n          'label': '공지 및 안내 관리',\n          'icon': Icons.campaign_outlined,\n          'page': NoticeManagementScreen(user: user),\n        },\n        {\n          'label': '하역 자료 관리',\n          'icon': Icons.view_column_outlined,\n          'page': const UnloadingListManagementScreen(),\n        },"
if m.count(ma)!=1: raise SystemExit('menu admin/staff anchor missing')
m=m.replace(ma,mn,1)
menu.write_text(m,encoding='utf-8')
shutil.copy2(src,dst)
print('Patch174A applied.')