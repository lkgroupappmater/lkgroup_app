// lib/core/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary / Navy
  // Shared header and bottom navigation color: #174B78.
  static const Color primary       = Color(0xFF174B78);
  static const Color navyPrimary   = Color(0xFF174B78);
  // Compatibility aliases used by the existing dashboard screen.
  static const Color primaryNavy   = navyPrimary;

  // Accent / Teal
  static const Color accent        = Color(0xFF00B4D8);
  static const Color tealAccent    = Color(0xFF00B4D8);
  // Compatibility alias used by the existing dashboard screen.
  static const Color accentTeal    = tealAccent;

  // Backgrounds
  static const Color background    = Color(0xFFF4F6FA);
  static const Color surface       = Color(0xFFFFFFFF);
  static const Color white         = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimary   = Color(0xFF1A1A2E);
  static const Color textSecondary = Color(0xFF5A6A85);
  static const Color textHint      = Color(0xFFADB5BD);

  // Input
  static const Color inputFill     = Color(0xFFF0F2F5);

  // Borders / Dividers
  static const Color divider       = Color(0xFFE2E8F0);
  static const Color cardBorder    = Color(0xFFD0D8E4);

  // Status
  static const Color success       = Color(0xFF28A745);
  static const Color warning       = Color(0xFFFFC107);
  static const Color error         = Color(0xFFDC3545);
  static const Color inProgress    = Color(0xFF17A2B8);

  // Tags
  static const Color tagBlue       = Color(0xFF3A86FF);
  static const Color tagOrange     = Color(0xFFFF6B35);
}




